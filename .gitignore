
# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.Python

# --- Virtual environment ---
.venv/
venv/
env/

# --- IDE / OS ---
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# --- Kivy ---
.kivy/
logs/
*.log


# --- Reference library (large local-only PDFs/EPUBs, never pushed) ---
Reference/
*.pdf
*.epub
*.djvu

# --- pytest ---
.pytest_cache/

# --- One-off analysis/debug scratch (never source) ---
_db*.py
_analyze_*.py
_*scratch*
_summary.txt
_test.txt

# --- Editor/backup detritus ---
*.bak
*.orig

# --- Runtime-generated trace logs (app writes these at runtime) ---
divination_trace.txt

# --- Swiss Ephemeris data files (large binaries, downloaded separately) ---
# Drop the official sepl_*.se1 / seas_*.se1 files into core/ephe/ for full
# precision. The engine falls back to the built-in Moshier ephemeris when
# these files are absent, so this folder is intentionally git-ignored.
core/ephe/*.se1
core/ephe/*.seas
# ...except the bundled Chiron data file, which ships with the app so Chiron
# works fully offline (see core/ephemeris.py auto-discovery). Keep tracked.
!core/ephe/seas_18.se1